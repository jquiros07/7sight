; Production only — code is baked into the image, not live-edited, so it's
; safe (and much faster) to skip re-checking file mtimes on every request.
opcache.enable=1
opcache.enable_cli=0
opcache.memory_consumption=256
opcache.max_accelerated_files=20000
opcache.validate_timestamps=0
